# 海豚社区 (Dolphin Community) — 全局架构

## 核心竞争力

**替用户决定风控，降低 Web3 交易门槛。**

信号 → 模拟交易 → 数据透明 → 用户跟单/托管 → AI 管理仓位风控

别人给信号让用户自己判断，海豚的 Agent 先跑模拟、跑出成绩再让你跟，跟了就不用管了。

## 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (Next.js 15)                      │
│                     api.hvip.ink                         │
├─────────────────────────────────────────────────────────┤
│  /app          │ 聊天 + Agent 选择 + 信号 + 资产管理       │
│  /api/chat     │ AI 对话路由 → DeepSeek V4               │
│  /api/wallet   │ 钱包代理 → MCP API                      │
│  /api/signals  │ 信号聚合 → MCP API                      │
├─────────────────────────────────────────────────────────┤
│                     MCP API (FastAPI)                     │
│                   MCP Server :3000                        │
├─────────────────────────────────────────────────────────┤
│  市场数据     │ CCXT → OKX 行情 + HURST/EMA/BB 分析       │
│  信号数据     │ OnchainOS signal list (真实链上)          │
│  Meme 扫描    │ OnchainOS memepump tokens                │
│  钱包操作     │ OnchainOS wallet login/send/swap/balance │
│  预测市场     │ Polymarket Gamma API + CLI                │
│  回测引擎     │ H1 BB 均值回归 / H3 Pivot 支撑狙击        │
│  后台 Worker  │ 6 Agent 7×24 自动巡检                     │
├─────────────────────────────────────────────────────────┤
│                外部数据源                                 │
├─────────────────────────────────────────────────────────┤
│  OnchainOS CLI  │ 链上信号/钱包/代币/Meme/Swap/桥        │
│  OKX (CCXT)     │ 合约行情/K线/持仓                       │
│  Polymarket     │ 预测市场赔率                            │
│  DeepSeek       │ LLM 推理 (Anthropic 兼容接口)           │
├─────────────────────────────────────────────────────────┤
│                Codex 模拟交易系统                          │
│          hwalletvip888-h.github.io/codex                  │
├─────────────────────────────────────────────────────────┤
│  signal-paper-trader │ 信号评分 → 模拟交易 → 黑名单       │
│  GitHub Actions      │ 每小时自动扫描                     │
│  每日策略总结        │ 自动归档经验教训                    │
└─────────────────────────────────────────────────────────┘
```

## Agent 体系

| Agent | 定位 | 职责 |
|--------|------|------|
| **小海豚** dolphin | 新手引导 + 平台管家 | 充币/提币/转账指引、安全科普、平台导航、钱包登录 |
| **链上猎手** onchain | 全链数据引擎 | 钱包查询、信号监控、Meme 扫描、跨链桥、Swap、Gas 站 |
| **诸葛策略** zhuge | 合约量化分析 | 行情分析、交易信号、多币扫描、策略回测、持仓管理 |
| **AI预言帝** worldcup | 预测市场 | Polymarket 赔率、下单、世界杯知识 |
| **稳盈管家** wealth | 多市场行情 | BTC/ETH/SOL 深度分析、资产配置建议 |
| **派奖福星** reward | 活动运营 | 活动列表、奖励领取、排行榜 |

### Agent 边界

- 充值/转账/入金/地址 → 统一引导到小海豚
- 行情/策略/回测/信号/持仓 → 诸葛策略
- 链上数据/钱包/Swap/桥/聪明钱 → 链上猎手
- AI 不编造地址、不猜测网络、不提供虚假安全审计

## 链上猎手 — 子页面

链上猎手聊天区顶部有四个子 Tab：

| Tab | 内容 |
|-----|------|
| **链上猎手** | AI 对话 |
| **当前持仓** | 待接入 OKX 真实持仓 |
| **模拟交易** | Agent 跟单说明 + codex 看板链接 |
| **信号** | 横向交易卡片流 + 垂直信号列表 (OnchainOS 链上数据) |

## 信号 → 模拟交易 → 跟单 流程

```
OnchainOS 链上信号
    │
    ▼
┌──────────────────┐
│  信号评分引擎      │
│  钱包黑名单过滤    │
│  多钱包共振检测    │
│  金额/市值筛选     │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  模拟交易执行      │
│  置信分→仓位       │
│  硬止损止盈        │
│  跟踪止损          │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  数据面板          │
│  历史模拟记录      │
│  盈亏透明          │
│  策略可见          │
└──────┬───────────┘
       │
       ▼
  用户选择
  ├── 手动跟单
  └── 自动托管 (Agent 管理风控)
```

## 风控体系

### 动态黑名单
- 钱包买入后币价暴跌 → 拉黑
- 多次被打脸的钱包 → 反向信号
- 每次操作后跟踪结果 → 更新钱包信用

### 仓位管理
- 评分决定仓位大小 (不是固定比例)
- 单币最大仓位硬限制
- 同时持仓数量上限

### 止损止盈
- 硬止损：-5%
- 硬止盈：+10%
- 跟踪止损：盈利 >5% 后启动，回撤 3% 离场

### 异常检测
- 币价 5 分钟波动 > 10% → 唤醒 Agent
- 黑名单钱包大额操作 → 反向警告

### 核心理念
> 要么赚的多，要么亏的少。两样占一样就有盈利，其他的用量来堆。

## 部署架构

| 服务 | 地址 | 方式 |
|------|------|------|
| 前端 Next.js | VPS :3101 | PM2 (fork) |
| MCP API | VPS :3000 | nohup python3.11 |
| Codex | GitHub Pages | GitHub Actions 小时级 |
| 域名 | api.hvip.ink | nginx 反代 |
| CI/CD | GitHub Actions | push main 自动部署前端 |

### 部署流程
```
git push → GitHub Actions build → SCP .next → SSH restart PM2
本地: ./deploy.sh (需要 FE_HOST/MCP_API_URL/DEEPSEEK_API_KEY 环境变量)
```

## 技术栈

| 层 | 技术 | 
|------|------|
| 前端框架 | Next.js 15 + React 19 |
| AI 模型 | DeepSeek V4 Pro (Anthropic 兼容 API) |
| 后端 | Python 3.11 + FastAPI |
| 行情数据 | CCXT (OKX) |
| 链上数据 | OnchainOS CLI |
| 预测市场 | Polymarket Gamma API |
| 样式 | Tailwind CSS + Framer Motion |
| 数据库 | SQLite (better-sqlite3) |
| 部署 | PM2 + nginx + GitHub Actions |

## 当前状态 & 下一步

| 模块 | 状态 | 
|------|------|
| 6 Agent 对话 | ✅ 已上线 |
| AI 意图路由 | ✅ meta/数据问题分离 |
| 钱包登录/充提兑 | ✅ 弹窗直连 |
| 资产面板 | ✅ 侧边栏展示 |
| 信号数据 | ✅ OnchainOS 真实链上 |
| 信号展示 | ✅ 横向卡片 + 垂直列表 |
| 模拟交易说明 | ✅ |
| 信号评分引擎 | 🔜 接 codex |
| 模拟交易执行 | 🔜 |
| 持仓数据 | 🔜 接 OKX |
| 用户跟单/托管 | 🔜 |
| 合约策略 | 🔜 |
| XSS/安全加固 | ✅ |
| Rate Limit | ✅ |
| API 认证 | ✅ MCP_API_KEY |
